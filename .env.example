# Only needed if you deploy the OPTIONAL serverless proxy in /api.
# The GitHub Pages build does not use this file at all.
# Copy to .env.local and fill in. NEVER commit the real file.

ANTHROPIC_API_KEY=
GEMINI_API_KEY=
